# Badtime War — 项目架构与编码规范

## 项目概述
Badtime War 是一个基于 Python 的回合制大逃杀桌游电子裁判系统。
包含 14 个天赋（7原初+7神代）、BasicAI 启发式策略引擎、
RL 训练管线（MaskablePPO+GRU）、联机对战（TCP+Textual TUI）、
以及 LLM/AIRI 角色聊天集成。

核心规则：所有设计和机制理解必须基于 README 中的描述。
不能经由名称自行猜测。任何效果实现必须与 README 相符。
如果不确定，直接问用户。

## 架构分层（禁止跨层调用）

engine/          — 游戏引擎核心。除非明确要求，禁止修改。
                   关键文件：game_state.py, round_manager.py, action_turn.py
combat/          — 伤害结算系统（22步流水线）。禁止随意修改核心流水线。
actions/         — 行动类型实现。每个文件导出 execute() 函数。
models/          — 数据模型（Player, Equipment, Markers, PoliceData）
talents/         — 天赋系统。所有天赋继承 BaseTalent (talents/base_talent.py)
controllers/     — 玩家控制器（Human, BasicAI, RL, Network, AIRI）
                   AI 使用 Mixin 模式，修改时注意 MRO 顺序
cli/             — 命令行解析（parser.py）与验证（validator.py）
locations/       — 地点交互逻辑
network/         — 联机网络层（TCP 协议，不可随意修改通信格式）
rl/              — 强化学习训练管线
ai_chat/         — LLM 聊天集成（可选模块）
tui/             — Textual TUI 界面
data/prompts.json — 所有面向用户的文本模板

## 核心接口契约

### Action 模块
所有 action 模块的 execute() 返回格式：
{"success": bool, "message": str, ...}

### BaseTalent Hook 方法（所有天赋继承此接口）
- on_register()                                    — 天赋注册时调用
- on_round_start(round_num)                        — R0 轮次开始
- on_round_end(round_num)                          — R4 轮次结束
- on_turn_start(player)                            — T0 回合开始
  返回 {"consume_turn": bool, "message": str} 或 None
- on_turn_end(player, action_type)                 — T2 回合结束
- get_t0_option(player)                            — 返回 T0 可用选项
  返回 {"name": str, "description": str} 或 None
- execute_t0(player)                               — 执行 T0 天赋
  返回 (message: str, consume_turn: bool)
- modify_outgoing_damage(attacker, target, weapon, base_damage)
- on_death_check(player, damage_source)
  返回 {"prevent_death": bool, "new_hp": float} 或 None
- on_crime_check(player_id, crime_type)
  返回 {"immune": bool} 或 {"extra_turn": bool}

Hook 覆盖规则：当前所有 hook 的基类默认实现为空。
子类可直接覆盖，不需要调用 super()。
但禁止修改 hook 的方法签名。

### PlayerController 接口
- get_command()  → 返回指令字符串
- choose(prompt, options) → 返回选项字符串
- confirm(prompt) → 返回 bool

### LLMBackend 接口
- chat(messages: List[Dict], temperature: float = 0.7) → str
  messages 格式：[{"role": "system/user/assistant", "content": "str"}]
  空字符串表示失败。

## 新增天赋检查清单（按顺序）

1. 创建天赋文件：talents/tX_xxx.py 或 talents/gX_xxx.py
2. 继承 BaseTalent，覆盖所需 hook 方法
3. 在 engine/game_setup.py 的 TALENT_TABLE 中注册
4. 如需 AI 优先选择：在 AI_TALENT_PREFERENCE 中添加偏好
5. 在 data/prompts.json 中添加 lore 文本和激活提示
6. 如需新指令：在 actions/ 中添加并在 action_registry.py 中注册
7. 如需新验证：在 cli/validator.py 中添加验证函数
8. 如需 AI 会使用：在 controllers/ai/ 对应 Mixin 中添加逻辑
9. 如需 RL 支持：更新 rl/action_space.py、rl/obs_builder.py、rl/reward.py

## 不可触碰区域

- engine/round_manager.py — 游戏循环时序逻辑（R0-R4/T0-T2 执行顺序）
- combat/damage_resolver.py — 22步伤害结算流水线
- engine/game_state.py — 全局状态管理
- 修改方式：通过 talent hook 扩展，而非修改核心逻辑

## 伤害结算流水线（22步，顺序不可变更）

1. 爱愿免疫检查 → 2. 电流免疫 → 3. 天赋修改输出伤害
→ 4. 计算原始伤害 → 5. 星野架盾过滤 → 6. 警察保护阈值
→ 7. 星野持盾减免 → 8. 星野被动保护 → 9. 萤火受伤减免
→ 10. 选择目标护甲 → 11. 属性克制判定 → 12. 伤害量化
→ 13. 扣减护甲 → 14. 全息影像额外伤害 → 15. 扣减HP
→ 16. 星野色彩10检查 → 17. 石化解除 → 18. 眩晕/死亡判定
→ 19. 电磁步枪震荡 → 20. 隐身失效 → 21. 愿负世积累火种
→ 22. 破除爱愿

属性克制规则：科技→魔法→普通→科技。被克制则整次攻击无效。
电流免疫：陶瓷护甲（immune_electric tag）完全免疫电流武器和震荡。

## 护甲系统边界规则

- 外层护甲未全破时攻击内层：攻击自动导向外层，禁止直接攻击内层
  （damage_resolver.py _select_armor_target）
- 最后一件内层护甲被击破：溢出伤害转移到 HP
  （_redirect_overflow_damage）
- 护甲属性克制攻击方武器：整次攻击无效（is_effective() 检查）
- 溢出重定向有多件候选护甲：优先同层，随机选择不免疫武器属性的护甲

## 警察系统

### 状态机
idle → reported（举报）→ assembled（集结）→ dispatched（出动）

### 威信机制
- 攻击无辜者：-1
- 队长犯罪：-1
- 归零：解除队长身份，原队长成为唯一违法者

### EQUIPMENT_LOCATION
警棍→警察局 | 高斯步枪→军事基地 | 魔法弹幕→魔法所 |
盾牌→商店/home | 陶瓷护甲→商店 | 魔法护盾→魔法所 |
AT力场→军事基地

## TALENT_TABLE（14个天赋）

| # | 类名 | 文件名 | Tier |
|---|------|--------|------|
| 1 | OneSlash | t1_one_slash.py | 原初 |
| 2 | ScissorRush | t2_scissor_rush.py | 原初 |
| 3 | Star | t3_star.py | 原初 |
| 4 | Hexagram | t4_hexagram.py | 原初 |
| 5 | Combo | t5_combo.py | 原初 |
| 6 | GoodCitizen | t6_good_citizen.py | 原初 |
| 7 | Resurrection | t7_resurrection.py | 原初 |
| 8 | G1MythFire | g1_firefly.py | 神代 |
| 9 | Hologram | g2_hologram.py | 神代 |
| 10 | Mythland | g3_mythland.py | 神代 |
| 11 | Savior | g4_savior.py | 神代 |
| 12 | Ripple | g5_ripple.py | 神代 |
| 13 | CutawayJoke | g6_cutaway.py | 神代 |
| 14 | Hoshino | g7_hoshino.py | 神代 |

### 天赋特有物品/状态
- G1 火萤：炽愿、超新星
- G4 愿负世：火种、临时HP
- G5 涟漪：爱愿、追忆
- G6 笑声：笑点、cutaway充能
- G7 星野：铁之荷鲁斯、荷鲁斯之眼、弹药、药物（EPO/巧克力等）、
  战术装备（烟雾弹/震撼弹/致盲弹等）、色彩值(0-10)、Terror状态

## BasicAI 架构

### Mixin 分层（注意 MRO 顺序，禁止改动）
- HoshinoMixin — 星野天赋专用
- HelpersMixin — 工具函数
- EvaluationMixin — 威胁评估（_estimate_power, _update_threat_scores）
- ChooseMixin — 选择逻辑
- CombatMixin — 战斗决策（_pick_target, _pick_weapon）
- DevelopMixin — 发育决策
- PoliceMixin — 警察相关
- EventsMixin — 事件响应

### 核心打分函数
目标选择：threat_score*2 + retaliation(+50) + location(+30)
          + hp_bonus((5-hp)*10) - armor_penalty(外-15,内-10)
武器选择：damage*10 + effective(+20) - countered(-50) ± range_bonus
          - charge_penalty(未蓄力-500)
战力估算：hp*10 + weapons*15 + outer_armor*20 + inner*15
          + stealth*10 + detection*5

### 已知行为异常（修 bug 时优先关注）
1. 星野看见队长应激（controller.py:652-683）
2. 火萤 debuff 后仍进危险模式（evaluation_mixin.py:18-55）
3. 政治人格 fallback 逻辑不完整

## RL 系统

### 训练流程
BC数据收集 → BC预训练 → 权重迁移 → PPO训练 → TorchScript导出

### Observation Space（523维）
| 索引 | 内容 | 维度 |
|------|------|------|
| 0-21 | 自身状态 | 22 |
| 22-31 | 武器拥有 | 10 |
| 32-38 | 护甲状态 | 7 |
| 44-228 | 对手状态（5×37） | 185 |
| 229-243 | 警察状态 | 15 |
| 287-300 | 自身天赋ID（one-hot） | 14 |
| 301-340 | 自身天赋状态 | 40 |
| 341-510 | 对手天赋（5×34） | 170 |
| 520-522 | choose模式指示器 | 3 |

### Action Space（130维离散）
0:forfeit | 1:wake | 2-7:move(6) | 8-34:interact(27) |
35-39:lock(5) | 40-44:find(5) | 45-94:attack(50) |
95-100:special(6) | 101-107:police(7) |
108-112:talent_t0_target(5) | 113:talent_t0_self |
114-129:choose_option(16)

### 奖励函数
- Terminal：获胜+100 死亡-100 平局-75
- Shaping：gamma*Phi(s')-Phi(s)
  势函数包含生存(HP×20+护甲)、发育(经济/武器)、战略(击杀)、
  战斗准备(面对/锁敌)、警察、天赋专属
- Event：命中+2 HP伤害×5 破甲+3 眩晕+4 击杀+30（天赋有专属值）
- Penalty：forfeit递增惩罚、连续移动惩罚(第3次-2,第4次-4...)、
  重复行动惩罚(第5次-4,第6次-8...)

## AIRI/LLM 集成

### 三种模式
1. 聊天皮肤（ai_chatter.py）：BasicAI决策，AIRI只负责聊天
2. 独立玩家（airi_controller.py）：AIRI自己做决策（WebSocket spark协议）
3. 外部桥接（bot_bridge.py）：TCP(游戏)↔WebSocket(AIRI)独立进程

### [ADJUST] 标签
正则提取→JSON解析→_apply_adjust()应用
- threat_mod：修改_threat_scores（范围[-20,+20]）
- alliance：修改_llm_alliance集合（内部记录）
- aggression：修改_llm_aggression_mod（单次[-10,+10]，累计[-20,+20]）
注意：目前无自动衰减机制

## 网络与多人

### 通信协议
Server→Client：REQUEST_COMMAND/CHOOSE/CONFIRM、GAME_EVENT、
  CHAT_MESSAGE、LOBBY_UPDATE、DISCONNECT_NOTICE
Client→Server：COMMAND_RESPONSE、CHOOSE_RESPONSE、CHAT_SEND、
  HEARTBEAT、RECONNECT

### 同步机制
NetworkController 使用 threading.Event + send_to_sync 实现
同步阻塞等待远程客户端响应。
引擎在同步线程中运行，网络层使用 asyncio（在独立线程中）。
使用 threading.Event 桥接两个世界。

### 断线处理
心跳超时：15秒（客户端每5秒发送）
WAIT_RECONNECT：切换到ForfeitController等待重连
AI_TAKEOVER：立即切换到AI控制器（优先RL模型，失败则BasicAI）

## 编码规范

1. 面向用户的文本必须走 data/prompts.json，禁止硬编码中文
2. 调试日志可硬编码，内部标识符使用英文
3. 文档字符串使用中文
4. 所有函数需完整类型注解
5. 单次修改不超过 5 个文件
6. 不要重写正常工作的代码
7. 优先使用已有方法，避免自创功能相同的新方法
8. 不确定的设计问题，直接问用户，不要自行猜测
9. 使用 UTF-8 编码处理所有中文字符串
10. prompts.json 顶层分类：ui/game/combat/talent/system/help/debug/error

## 常见 Bug 模式（开发时警惕）

1. **天赋hook优先级错误**：修改BaseTalent方法签名但未更新所有子类
2. **伤害流水线遗漏**：在damage_resolver中添加逻辑但未考虑talent hook优先级
3. **额外回合无限循环**：触发条件设置不当。防循环措施：回合计数器、
   条件检查、状态标记。新增防循环逻辑应加在round_manager._phase_r3中
4. **字符串编码**：未使用UTF-8
5. **网络同步**：threading.Event未正确set或消息丢失

## 游戏卡住排查

1. threading.Event未set → 检查network/server.py的_sync_events
2. action_queue不空但所有玩家死了 → 检查round_manager.py
3. 天赋额外回合无限链 → 检查round_manager.py R3阶段插入逻辑

## 测试方式

- 集成测试：python stats_runner.py --players 6 --games 500
- 单机热座：python main.py
- 联机测试：python main_server.py --port 9527 --players 4
- 调试日志级别：1=基本 2=详细 3=完整
- 日志位置：logs/ 目录，格式 {timestamp}_{numH}H_{numAI}AI_{talent}.log

## 入口脚本

| 脚本 | 用途 |
|------|------|
| main.py | 本地单机热座 |
| main_server.py --port 9527 --players N | 联机房主 |
| main_client.py --host 127.0.0.1 --port 9527 | 联机客户端 |
| stats_runner.py --players 6 --games 500 | 自动胜率统计 |

## 禁止事项

1. 不要在 talent 中直接 import 另一个 talent 模块
2. 不要绕过 cli.validator 直接修改 GameState
3. 不要修改 BaseTalent 的方法签名
4. 不要改动 engine/ 的时序逻辑（R0-R4/T0-T2）
5. 不要改变 Mixin 组合顺序（影响 MRO）
6. 不要在游戏循环中使用阻塞操作
7. 不要在非思考模式下做需要跨文件推理的设计决策